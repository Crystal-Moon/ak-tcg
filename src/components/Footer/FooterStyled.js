/* eslint-disable */
import { Grid } from '@material-ui/core';

export function FooterStyled(props) {
  const { lang } = props;
  return (
    <Grid container spacing={1}>
      <div>Footer</div>
    </Grid>
  );
}
