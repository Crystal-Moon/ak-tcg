/* eslint-disable */
import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {},
  formContainer: {},
  cardContainer: {
    minWidth: 380,
  },
});
